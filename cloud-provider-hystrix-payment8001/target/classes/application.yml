server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true
    register-with-eureka: true

spring:
  application:
    name: cloud-provider-hystrix-payment
